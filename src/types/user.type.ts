export type userState = {
    data: {

        message: null | string,
        user: {
            _id: string,
            createdAt: string,
            dateOfBirth: string,
            email: string,
            gender: string,
            name: string,
            photo: string
        }
    }
}